<%= form_for(@ciudad, html: { multipart: true, :remote => true  }) do |f| %>


<% if @ciudad.errors.any? %>
  <div id="error_explanation">
    <h2>Por Favor Verificar:</h2>

    <ul>
    <% @ciudad.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>



<div class="user">
  <div class="modal-header">
    <h4 class="modal-title"><h3>Datos ciudad</h3></h4>
  </div><br>
  <div class="row">
    <div class="col-md-4">
      Nombre:
    </div>
    <div class="col-md-6">
      <%= f.text_field :nombre, :label => false, class: "form-control" %>
    </div>

  </div>
</div>
<div class="modal-footer">
  <%= f.submit "Guardar", :class => 'btn btn-primary' %>
  <a href="#" class="btn btn-default" onclick="$.lazybox.close()">Cerrar</a>
</div>
<% end %>